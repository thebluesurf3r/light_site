// static/dir_scan_vis/js/app.js

document.addEventListener("DOMContentLoaded", () => {
    const searchInput = document.querySelector('input[name="search"]');
    
    // Example: Autofocus on the search input
    if (searchInput) {
        searchInput.focus();
    }
});